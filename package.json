{
  "name": "apace-ticket",
  "version": "1.0.0",
  "description": "Ticket management system monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd apps/api && npm run start:dev",
    "dev:web": "cd apps/web && npm run dev",
    "build": "npm run build:api && npm run build:web",
    "build:api": "cd apps/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "test": "npm run test:api && npm run test:web",
    "test:api": "cd apps/api && npm run test",
    "test:web": "cd apps/web && npm run test",
    "lint": "npm run lint:api && npm run lint:web",
    "lint:api": "cd apps/api && npm run lint",
    "lint:web": "cd apps/web && npm run lint",
    "docker:up": "cd infra && docker compose up -d",
    "docker:down": "cd infra && docker compose down",
    "docker:build": "cd infra && docker compose up --build",
    "setup": "npm install && npm run docker:up && sleep 15 && npm run migrate && npm run seed",
    "migrate": "cd apps/api && npm run migration:run",
    "seed": "cd apps/api && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
