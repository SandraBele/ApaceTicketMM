# Next.js Configuration
# Note: API calls use same-origin proxy via Next.js rewrites
# No NEXT_PUBLIC_API_URL needed - all requests go to /api/* and are proxied to the backend

# Node Environment
NODE_ENV=development
