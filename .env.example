# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=apace_ticket

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# API
PORT=4000
NODE_ENV=development

# Frontend
# No environment variables needed - all API calls use same-origin proxy (/api/*)
